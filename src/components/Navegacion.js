import React from "react";
import { Navbar } from "react-bootstrap";

function Navegacion() {
     return (
          <Navbar bg="dark" variant="dark">
               <Navbar.Brand href="#home">Portfolio / Curriculum </Navbar.Brand>
          </Navbar>
     );
}

export default Navegacion;
